import { NotFoundCard } from "@/components/NotFoundCard";

/** notFound() on a page (deleted player, tournament, club night): keeps the header and footer. */
export default function NotFound() {
  return <NotFoundCard />;
}
